# 喵汪待办 · 电脑网页版

## 最简单的打开方式

双击项目文件夹里的 `打开电脑网页版.command`，浏览器会自动打开：

`http://127.0.0.1:4173`

第一次双击如果 macOS 阻止运行，请右键这个文件，选择“打开”，再确认一次。

## 网页端包含的功能

- 今日、本周、本月进度，其中今日使用完整进度条。
- 今日唯一必做、5 分钟启动、现在／下一步／稍后。
- 今日任务可在“按板块”中拖动或上下移动排序，时间线会同步采用手动顺序。
- 待办新增、编辑、完成记录、批量删除和自动顺延。
- 独立工作流：项目、细颗粒度任务、Waiting、Notes、Timeline、筛选与项目归档。
- 工作流任务可以手动关联一个首页事项；首页默认收起，只显示关联数量。
- 日历、统计、五象生活、近七日图表和优先级分布。
- 周期批量计划、想法收集箱、提醒伙伴和鸡汤文库。
- DeepSeek 大任务拆解、根据粘贴文本拆解、参考网址、可阅读方案文档。
- 周概览审核、每周继续细化为每日计划、整项大计划删除。

网页数据保存在当前浏览器的本地存储中。清除浏览器网站数据会删除待办，建议定期在“我的 → 导出数据”保存备份。

## 从本地版迁移到 HTTPS 在线版

`file://`、`http://127.0.0.1:4173` 和未来的 `https://...` 是不同来源，浏览器不会自动共享 localStorage。

安全迁移步骤：

1. 在仍能看到旧数据的本地地址打开应用。
2. 进入“我的 → 导出数据”，保存 JSON 文件。
3. 打开新的 HTTPS 网站。
4. 进入“我的 → 导入数据”，选择刚才的 JSON。
5. 确认导入。系统会先自动下载一份线上当前数据备份，再覆盖并重新载入。

每个浏览器、设备和网站来源的数据彼此独立；把网址发给朋友后，朋友会获得自己的本地数据，不会看到你的待办。

## PWA 安装和离线

- Safari（Mac）：打开 HTTPS 网站后，选择“文件 → 添加到程序坞”。
- Chrome：点击地址栏右侧的安装图标，或选择“安装喵汪待办”。
- 也可以进入应用“我的 → 安装到桌面 / Dock”查看提示。

安装后应用使用 standalone 独立窗口。首次在线打开成功后，首页、样式和脚本会被缓存，可基础离线打开；DeepSeek AI 规划和未缓存的网络能力仍需要联网。

在“我的 → 提醒小伙伴”中开启系统通知后，只要网页 App 仍在运行（包括最小化到程序坞），应用会每 30 秒检查一次到点任务并发送 macOS 系统通知。受系统节能和浏览器调度影响，提醒可能有少量延迟；完全退出网页 App 后不会提醒。

在桌面版首页点击任意待办，会在右侧打开“行动拆解”面板。可在文本框中每行输入一个小步骤并一次加入，之后逐项勾选、删除或清除已完成步骤。小步骤跟随主待办保存在同一份浏览器本地数据中，也会包含在 JSON 导出和导入中。

## GitHub Pages 发布

项目保留原生 HTML/CSS/JS，不引入前端框架。`.github/workflows/pages.yml` 会在 `main` 分支更新后自动发布静态 PWA。GitHub Pages 需要在仓库设置中选择 GitHub Actions 作为发布来源。

## 让网页调用现有 DeepSeek 云函数

网页已经接入 CloudBase Web SDK，并使用同一个环境：

`cloud1-d4gou1my5b1ea5ee2`

DeepSeek API Key 仍只保存在 `aiPlanner` 云函数环境变量里，不会写进网页。

正式上线前需要在云开发控制台完成：

1. 开启匿名登录，或创建一个浏览器可用的 Publishable Key。
2. 将正式网页域名加入 Web 安全域名。
3. 如果使用 Publishable Key，把它填写到 `web-config.js` 的 `accessKey`。
4. 确认匿名角色只允许调用 `aiPlanner` 所需能力，不开放数据库写入权限。

## 可直接发布的静态文件

- `index.html`
- `styles.css`
- `app.js`
- `web-config.js`
- `manifest.webmanifest`
- `sw.js`
- `icons/`

这些文件由 GitHub Pages 工作流发布。网页源码中不要加入 `DEEPSEEK_API_KEY`。
